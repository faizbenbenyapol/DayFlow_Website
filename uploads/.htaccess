Options -Indexes

<IfModule mod_headers.c>
    Header always set X-Content-Type-Options "nosniff"
</IfModule>

# Uploads are data only and must never execute server-side code.
<FilesMatch "\.(php|php[0-9]?|phtml|phar|cgi|pl|py|sh|rb|asp|aspx|jsp)$">
    Require all denied
</FilesMatch>
