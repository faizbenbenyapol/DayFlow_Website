# Changelog — DayFlow

บันทึกการเปลี่ยนแปลงและการพัฒนาของแพลตฟอร์ม **DayFlow** ระบบบริหารจัดการประสิทธิภาพการทำงาน (Productivity), การวางแผนงาน, บันทึกสุขภาพ และการบริหารจัดการการเงินแบบครบวงจร

---

## [Unreleased]

รอบนี้มาจากการตรวจโค้ดทั้งระบบ: ปิดช่องโหว่ร้ายแรงที่พบ แก้บั๊กที่ซ่อนอยู่ และจัดโครงสร้างใหม่ โดยทุกขั้นตรวจด้วยชุดทดสอบ (319 ข้อ), PHPStan และการเทียบภาพหน้าจอก่อน/หลังทีละพิกเซล

### ต้องทำตอน deploy

*   **Rebuild image** — Apache อ่าน IP จริงของผู้ใช้จาก proxy (`docker/remoteip.conf`) และ document root ย้ายไปที่ `public/`
*   **รัน `php scripts/migrate.php`** — migration ย้ายไปอยู่ที่ `sql/migrations/NNN_*.sql` ฐานข้อมูลเดิมถูกจับคู่กับชื่อไฟล์เก่าอัตโนมัติ มี migration ใหม่ 3 ไฟล์ และแอปไม่สร้างตารางเองระหว่างใช้งานอีกแล้ว
*   ถ้าไม่ใช้ Docker ให้ชี้ document root ไปที่ `public/` (ดู `DEPLOYMENT.md`)

### ความปลอดภัย (Security)

*   **ลิงก์แชร์เมนู export ข้อมูลทั้งบัญชีของเจ้าของได้** (`/api/settings/export`) และเห็นสรุปแดชบอร์ดทุกโมดูล
*   **SQL injection ใน Import** — ชื่อคอลัมน์มาจาก key ใน JSON ตรง ๆ และนำเข้าบล็อกโน้ตลงโน้ตของผู้ใช้อื่นได้
*   **ไฟล์ทั้ง repository เปิดผ่านเว็บได้** — `scripts/migrate.php`, `tests/`, log, README, compose ตอนนี้เสิร์ฟเฉพาะ `public/`
*   **Stored XSS ผ่านรูปหน้าจอหุ้น** — ระบบเชื่อชนิดไฟล์และนามสกุลที่เบราว์เซอร์ส่งมา และไฟล์ใน `uploads/` เปิดตรงได้
*   **XSS จากชื่อไฟล์ใน ZIP**, ผู้รับผิดชอบงานในโปรเจค, หน้า Skills และชื่อแท็กที่ปิด `<script>` ก่อนเวลา
*   **ล็อกอินด้วย Google ข้าม 2FA ได้**
*   **บัญชี demo** อัปโหลดไฟล์ สร้างลิงก์สาธารณะ และแก้การตั้งค่าได้
*   **Rate limit ใช้ไม่ได้จริงหลัง proxy** (ทุกคนนับเป็น IP เดียว) เพิ่ม limit ให้การสมัครสมาชิกและรหัส File Transfer
*   Role ของโปรเจครับค่าอะไรก็ได้, สคริปต์จาก CDN ไม่มี SRI

### แก้ไขบั๊ก (Bug Fixes)

*   **สำรองข้อมูลแล้วกู้คืนไม่ครบ** — Export/Import ใช้รายชื่อตารางคนละชุด ตอนนี้ครอบคลุมทุกโมดูลส่วนตัว แทนที่เฉพาะส่วนที่มีในไฟล์ และกู้คืนข้ามบัญชีได้
*   **ข้อความที่มี `<` หายไป** — `strip_tags` ตัด "a<b" เหลือ "a" ตอนนี้เก็บตามที่พิมพ์และ escape ตอนแสดงผล
*   **บันทึกโดยไม่แก้อะไรแล้วได้ "ไม่พบรายการ"** และแก้ข้อมูลของคนอื่นแล้วได้ "สำเร็จ" (`MYSQL_ATTR_FOUND_ROWS`)
*   **เมนู "นิสัยประจำวัน" หายจาก sidebar** และไอคอนเมนูทุกอันกลายเป็นแบบเดียวกัน
*   โปรเจคที่เสร็จ 100% ไม่เคยได้ข้อความ "เสร็จสมบูรณ์", ลำดับสถิติออกกำลังกายสลับเอง, เครื่องมือ PDF ถูก CSP บล็อก, ปุ่มย้อนกลับในหน้า error ใช้ไม่ได้, การแจ้งเตือนผ่านเบราว์เซอร์เปิดใน production ไม่ได้, class `text-center`/`text-right`/`text-danger` ที่ใช้อยู่แต่ไม่เคยมีผล

### โครงสร้างและคุณภาพโค้ด

*   PHPStan ระดับ 5 ใน CI (ผ่าน 0 error)
*   Controller ขนาดใหญ่แยกตามหน้าที่ (ใหญ่สุดเหลือ 459 บรรทัด) และรวมโค้ดเรียก AI และ HTTP ที่ซ้ำกันไว้ที่ `LlmClient`/`HttpJson`
*   JavaScript: `escHtml` ตัวเดียวแทน 21 ชุด และแยก `projects.js`, `stocks.js`, `settings.js` ตามฟีเจอร์
*   CSS: inline style 754 → 551, สีที่ใช้ซ้ำเป็น palette token (สี hardcode 351 → 162), ลบโค้ดที่ไม่ได้ใช้ (`if (false)` 130 บรรทัด, web installer, `Response::view`)
*   รวมคู่มือ deploy เหลือ `DEPLOYMENT.md` ไฟล์เดียว และเลิก track `.claude/`, `.vscode/`, `.antigravity/`

---

## [1.4.0] - 2026-09-21

รอบนี้เน้นสามเรื่อง: ความเร็วของทุกคำขอ, ชุดทดสอบอัตโนมัติที่ครอบคลุม API ทั้งหมด และการปิดช่องโหว่ที่พบระหว่างทาง พร้อมฟีเจอร์ใหม่อีก 9 อย่าง

### ความปลอดภัย (Security)

*   **บังคับใช้ Content Security Policy:** เดิมตั้งเป็น Report-Only มาตลอดเพราะหน้าเว็บเต็มไปด้วย inline event handler ซึ่งทำให้ห้าม inline script ไม่ได้ — Report-Only เก็บรายงานแต่ไม่ได้ป้องกันอะไรเลย แปลง handler ครบทั้ง **239 จุด** (169 จุดใน template + 70 จุดที่ JavaScript สร้าง) เป็น attribute แบบประกาศ (`data-act`, `data-nav`, `data-click`, `data-stop`, `data-prevent`) โดยมี listener กลางตัวเดียวใน `app.js` เป็นผู้เรียก ส่วน inline `<script>` ที่แอปใช้จริง 13 บล็อกใส่ `nonce` ต่อ response ผลคือ `script-src` ไม่มีทั้ง `'unsafe-inline'` และ `'unsafe-eval'` อีกต่อไป และเพิ่ม `object-src 'none'`
*   **ผู้เยี่ยมชมแบบอ่านอย่างเดียวโพสต์ลงแชตโปรเจคได้:** การตรวจสิทธิ์ถูกเขียนซ้ำในแต่ละ endpoint ที่ต้องใช้ มีครบใน 4 endpoint ของงาน แต่ไม่มีในแชต ใครก็ตามที่ถือลิงก์แชร์ระดับ "Viewer" รวมถึงผู้ที่ไม่ได้ล็อกอิน จึงเขียนข้อความลงแชตได้ รวมการตรวจสิทธิ์เป็น `requireEditor()` และ `requireOwner()` แล้วใช้ให้ครบทั้ง 17 endpoint
*   **แฮชรหัสผ่านของโน้ตหลุดไปฝั่งเบราว์เซอร์:** `POST /api/notes` ส่งทั้งแถวกลับไป รวม `password_hash` และ `encrypt_salt` ของโน้ตที่เข้ารหัส ทำให้นำแฮช bcrypt ไปแครกแบบออฟไลน์ได้
*   **ยืนยันตัวตนสองชั้น (2FA):** รองรับแอป Authenticator ตามมาตรฐาน TOTP (RFC 6238) พร้อม QR Code และรหัสสำรองแบบใช้ครั้งเดียว

### แก้ไขบั๊ก (Bug Fixes)

*   **ดาวน์โหลดไฟล์แล้วได้ชื่อ "download" ทุกครั้ง:** `downloadFilename()` ใช้ `/` เป็นตัวคั่น regex ทั้งที่มี `/` ที่ไม่ได้ escape อยู่ข้างใน pattern จึงปิด pattern ก่อนเวลา `preg_replace` คืน null และไฟล์ทุกไฟล์เสียทั้งชื่อและนามสกุล
*   **เปลี่ยนรหัสผ่านไม่ได้เลย:** `User::findById()` ไม่ได้ดึงคอลัมน์ `password_hash` ทำให้ `apiPassword()` ส่ง null เข้า `verifyPassword()` แล้วคำขอตายด้วย TypeError
*   **ชื่อไฟล์ภาษาไทยเพี้ยนตอนดาวน์โหลด:** ส่งชื่อจริงเป็น UTF-8 ดิบใน `filename="..."` ซึ่ง RFC 6266 กำหนดให้อ่านเป็น latin-1 เบราว์เซอร์จึงต้องเดาเอง รวมการสร้าง header ทั้ง 11 จุดไว้ที่ `contentDisposition()` ที่ส่งชื่อจริงผ่าน `filename*` แบบ UTF-8
*   **ข้อมูลเก่าเข้าถึงไม่ได้:** รายการออกกำลังกาย โฟกัส ประวัติ AI และบันทึกทักษะถูกจำกัดไว้ที่ 50/50/30/50 รายการโดยไม่มีทางขอหน้าถัดไป เมื่อข้อมูลเกินจำนวนนั้น ของเก่าไม่ได้แค่ช้า แต่เปิดดูไม่ได้เลย
*   **ตอบว่าสำเร็จทั้งที่ไม่ได้แก้อะไร:** 7 endpoint เรียกคำสั่งลบหรือแก้ของโมเดลแล้วทิ้งค่าที่คืนมา ตอบ `ok` เสมอแม้ id นั้นไม่ใช่ของผู้เรียก (ข้อมูลปลอดภัยเพราะ SQL จำกัดด้วย `user_id` อยู่แล้ว แต่หน้าจอลบแถวออกทั้งที่ยังอยู่)
*   **ต่ออายุรายการของคนอื่นตอบผิดรหัส:** `renew()` คืน false ทั้งกรณี "ไม่ใช่ของคุณ" และ "รายการครั้งเดียวต่อไม่ได้" แยกให้ตอบ 404 กับข้อความที่ตรงเหตุผลจริง
*   **ข้อผิดพลาดที่ไม่ได้คาดคิดหลุดถึงหน้าเว็บ:** ไม่มี exception handler กลาง อัปโหลดไฟล์ที่ชื่อไม่ใช่ UTF-8 แล้วได้ stack trace เต็มพร้อม path ของเซิร์ฟเวอร์ และไม่มีบันทึกว่าคำขอไหนเป็นต้นเหตุ
*   **Migration ขาดไป 2 ไฟล์:** `docker-compose` ไม่ได้รัน `migrate_theme_colors` และ `migrate_demo_account` ฐานข้อมูลใหม่จึงขาดทั้งสองอย่าง

### ประสิทธิภาพ (Performance)

*   **เปิด OPcache และ JIT:** เดิมไม่มีการแคช opcode เลย ทุกคำขอคอมไพล์ Router, Controller, Model และ View ใหม่ทั้งหมด — หน้า `/login` เร็วขึ้นจาก 7.4ms เหลือ 3.0ms
*   **ปล่อย Session Lock ใน API ที่อ่านอย่างเดียว:** PHP ล็อกไฟล์ session ตลอดคำขอ หน้าที่ยิง XHR หลายเส้นพร้อมกันจึงต้องต่อคิวกันแทนที่จะวิ่งขนาน
*   **แดชบอร์ดสร้างเฉพาะ Widget ที่เปิดอยู่:** Widget หุ้นที่ถูกซ่อนยังคำนวณมูลค่าพอร์ตทั้งพอร์ตทุกครั้ง — `/api/dashboard/summary` ลดจาก 13 query เหลือ 2-3 query
*   **แก้เงื่อนไขวันที่ที่ทำให้ Index ใช้ไม่ได้ 14 จุด:** การห่อคอลัมน์ด้วย `DATE()`/`DATE_FORMAT()`/`YEAR()` บังคับให้ MySQL ไล่ประเมินทีละแถวและเมิน index
*   **ค้นหาเหลือ query เดียว:** จากเดิม 5 query ต่อการพิมพ์หนึ่งครั้ง พร้อมยกเลิกคำขอเก่าไม่ให้ผลลัพธ์ที่มาช้าทับผลใหม่
*   **โหลดไลบรารีเท่าที่ใช้:** SweetAlert2 (~90KB) โหลดตอนเปิด dialog ครั้งแรกเท่านั้น และ Sortable โหลดเฉพาะ 5 หน้าที่มีการลากวาง
*   **Self-host ฟอนต์ทั้งหมด:** `app.css` เดิมเปิดด้วย `@import` ของ Google Fonts ซึ่งเป็นรูปแบบที่ช้าที่สุด เบราว์เซอร์ต้องโหลดและ parse `app.css` จบก่อนจึงรู้ว่ามี stylesheet ฟอนต์อยู่ ตอนนี้ไม่มีคำขอไปภายนอกเลย
*   **เปิด mod_deflate:** การ deploy ด้วย Apache ล้วนไม่เคยบีบอัดเลย (`app.css` 32KB เหลือ 6.4KB)
*   **อ่านรายการยาวทีละหน้า:** เพิ่ม `Paginator` ใช้กับ 7 endpoint ที่ข้อมูลสะสมไม่มีเพดาน พร้อมปุ่ม "โหลดเพิ่ม" ในตารางการเงิน
*   **Class Autoloader:** คำขอหนึ่งไม่ต้องคอมไพล์ Model และ Helper ที่ไม่ได้ใช้อีกต่อไป

### เพิ่มฟีเจอร์ (New Features)

*   **งานและกิจกรรมที่ทำซ้ำ:** งานติ๊กเสร็จแล้วสร้างรอบถัดไปให้อัตโนมัติแบบเดียวกับการต่ออายุ Subscription ส่วนปฏิทินขยายรอบตอนอ่าน ทำให้กิจกรรมหนึ่งแถวแสดงได้ทุกวันที่ตรงเงื่อนไข (รายเดือนที่ตรงกับวันที่ 31 จะเลื่อนมาเป็นวันสุดท้ายของเดือนสั้นแทนที่จะข้ามไปเดือนถัดไป)
*   **นำเข้าและส่งออกปฏิทินเป็น .ics:** รองรับ RRULE และกิจกรรมทั้งวัน ใช้ร่วมกับ Google Calendar, Apple Calendar และ Outlook ได้
*   **หน้าสรุปผล (`/review`):** ภาพรวมรายสัปดาห์และรายเดือนข้ามโมดูล — งาน โฟกัส นิสัย ออกกำลังกาย การเงิน และโน้ต
*   **แจ้งเตือนผ่านเบราว์เซอร์ (Web Push):** ใช้ VAPID ของตัวเองและไม่ส่ง payload ผู้ให้บริการ Push จึงรู้เพียงว่า "ให้ปลุกเบราว์เซอร์นี้" ส่วนเนื้อหาถูกดึงจากเซิร์ฟเวอร์ของผู้ใช้เอง
*   **แถบคำสั่ง (Ctrl/Cmd + K):** อ่านปลายทางจาก Sidebar ที่เซิร์ฟเวอร์ render จึงเคารพเมนูที่ซ่อนไว้และโหมดแชร์โดยอัตโนมัติ
*   **ธีมตามระบบปฏิบัติการ:** สลับก่อนการวาดครั้งแรก ไม่มีจอกระพริบ
*   **ค้นหาครอบคลุม 12 โมดูล:** จากเดิม 5 โมดูล และค้นถึงเนื้อหาในโน้ตได้ โดยกันโน้ตที่เข้ารหัสไว้ไม่ให้ปรากฏ
*   **Service Worker และ PWA:** แคชเฉพาะไฟล์สแตติก ไม่แคชหน้าเว็บหรือ API เพราะเป็นข้อมูลส่วนบุคคลและเครื่องอาจใช้ร่วมกัน พร้อมหน้าออฟไลน์และไอคอนสำหรับติดตั้ง

### โครงสร้างและคุณภาพ (Infrastructure)

*   **ชุดทดสอบอัตโนมัติ 267 รายการ:** ครอบคลุม API ทุกเส้นทาง แบ่งเป็น Unit และ Integration ที่ขับแอปจริงผ่าน HTTP พร้อม session และ CSRF โดยไม่ต้องพึ่ง Composer
*   **GitHub Actions:** ตรวจ syntax ทุกไฟล์ PHP, รัน Unit และรัน Integration บน docker ที่ migrate ใหม่ตั้งแต่ต้น
*   **บันทึกสถานะ Migration:** ตาราง `schema_migrations` พร้อม checksum และคำสั่ง `--status` เดิมฐานข้อมูลที่ migrate ไปครึ่งทางดูไม่ต่างจากฐานข้อมูลใหม่เลย
*   **Dev Stack ไม่ต้อง rebuild:** bind-mount ซอร์สพร้อมเปิด OPcache revalidate ในโหมด dev
*   **Rate Limiter ลบไฟล์เก่าเอง:** เดิมสะสมไปเรื่อยๆ ไม่มีอะไรลบ

---

## [1.3.0] - 2026-07-27

### แก้ไขบั๊ก (Bug Fixes)

*   **ธีมใช้ได้แค่ 2 สี:** คอลัมน์ `theme` ในฐานข้อมูลถูกจำกัดเป็น `ENUM('light','dark')` ทั้งที่หน้าตั้งค่ารองรับ 6 ธีม ทำให้เลือกธีมพาสเทลอื่นแล้วบันทึกไม่สำเร็จ ขยาย ENUM ให้รองรับครบทั้ง 6 ธีม (`sql/migrate_theme_colors.sql`)
*   **โหมดแชร์เด้งไปหน้าล็อกอินหลังปิดแอป/กดย้อนกลับบนมือถือ:** ลิงก์ App Share ไม่แนบ Token ไว้ใน URL ปลายทางหลัง Redirect ทำให้ Shortcut ที่บันทึกไว้บนมือถือใช้งานไม่ได้อีกเมื่อ Session หมดอายุ แก้ให้ URL ปลายทางแนบ `?share=token` เสมอ เพื่อให้ Shortcut กู้คืนสถานะโหมดแชร์ได้เองโดยไม่ต้องล็อกอินใหม่

### เพิ่มฟีเจอร์ (New Features)

*   **ปุ่ม Demo หน้าล็อกอิน:** เพิ่มปุ่ม "Demo" ให้ผู้เยี่ยมชมเข้าดูตัวอย่างระบบแบบอ่านอย่างเดียวได้ทันทีโดยไม่ต้องสมัครสมาชิกหรือเข้าสู่ระบบ (`/demo`) ทำงานผ่านกลไก App Shares เดิม พร้อมล็อกไม่ให้บัญชี Demo ถูกล็อกอินตรงด้วยรหัสผ่านหรือ Google เพื่อความปลอดภัย

---

## [1.0.0] - 2026-05-31

การเปิดตัวเวอร์ชันอย่างเป็นทางการครั้งแรก (**v1.0.0**) ซึ่งประกอบด้วยฟังก์ชันการทำงานที่ครบถ้วน ระบบรักษาความปลอดภัยในการเข้าสู่ระบบ และการปรับปรุงสถาปัตยกรรมการออกแบบส่วนติดต่อผู้ใช้ (UI/UX) ให้มีความทันสมัย สวยงาม และรองรับการแสดงผลบนอุปกรณ์ทุกประเภทได้อย่างสมบูรณ์แบบ (Fully Responsive Design)

### ฟีเจอร์หลัก (Core Features)

*   **Dashboard (ระบบแดชบอร์ดหลัก):** ศูนย์รวมการแสดงผลกิจกรรมประจำวัน, โน้ตย่อเร่งด่วน, สถานะการดำเนินโครงการ รวมถึงวิดเจ็ตแสดงสถิติทางการเงินและสุขภาพ เพื่อให้ผู้ใช้สามารถประเมินภาพรวมรายวันได้อย่างมีประสิทธิภาพ
*   **Notes (ระบบจดบันทึกอัจฉริยะ):** ระบบบันทึกข้อความแบบจัดโครงสร้างบล็อกอิสระ (Text, Link, Checklist) รองรับการปักหมุดบันทึกสำคัญ การจัดหมวดหมู่ผ่านแท็ก และระบบเข้ารหัสข้อมูลบันทึก (Note Encryption) เพื่อรักษาความเป็นส่วนตัวขั้นสูง
*   **Project & Task Management (ระบบบริหารจัดการโครงการ):** บอร์ดบริหารงานรูปแบบคัมบัง (Kanban Board) ที่รองรับการเคลื่อนย้ายการ์ดงานแบบลากวาง (Drag-and-Drop) พร้อมระบบระบุระดับความสำคัญ (Priority) และกำหนดวันส่งมอบงาน (Due Date)
*   **AI Assistant (ระบบผู้ช่วยอัจฉริยะ):** บริการวิเคราะห์ข้อมูลและโต้ตอบด้วยปัญญาประดิษฐ์ (AI) สนับสนุนการสรุปเนื้อหาบันทึก ปรับเปลี่ยนระดับภาษา แนะนำแนวทางการดำเนินงาน และตอบข้อซักถามแบบเรียลไทม์
*   **Finance & Budget Tracker (ระบบบริหารการเงิน):** เครื่องมือบันทึกรายรับ-รายจ่ายส่วนบุคคล ติดตามประวัติการทำธุรกรรมรายวันและรายเดือน พร้อมระบบสรุปวิเคราะห์งบประมาณ
*   **Stocks Portfolio (ระบบติดตามพอร์ตการลงทุน):** ระบบจำลองการลงทุน ติดตามมูลค่าพอร์ตการลงทุนส่วนบุคคล และดึงข้อมูลราคาสินทรัพย์ เพื่อสนับสนุนการวางแผนกลยุทธ์ทางการเงิน
*   **Exercise & Food Notes (ระบบบันทึกสุขภาพ):** เครื่องมือบันทึกตารางการออกกำลังกาย และระบบบันทึกคุณค่าทางโภชนาการรวมถึงแคลอรีประจำวัน เพื่อการส่งเสริมสุขภาพอย่างมีระบบ
*   **File Manager & File Tools (ระบบจัดการไฟล์และเอกสาร):** คลังจัดเก็บข้อมูลส่วนบุคคลสำหรับอัปโหลดเอกสาร พร้อมเครื่องมือจัดการรูปภาพ แปลงไฟล์ และบริการแบ่งปันเอกสารภายนอก
*   **Collaboration & Link Sharing (ระบบการแบ่งปันข้อมูล):** ฟังก์ชันแบ่งปันบันทึกและเอกสารแก่บุคคลภายนอกผ่านเครือข่ายอินเทอร์เน็ต โดยสามารถสร้างลิงก์สาธารณะและกำหนดระยะเวลาหมดอายุของลิงก์เพื่อความปลอดภัย

---

### การปรับปรุงดีไซน์และธีม (UI/UX & Themes)

*   **Neumorphism & Soft UI Integration:** ปรับเปลี่ยนสไตล์การออกแบบให้มีความหรูหราและมีความเป็นมืออาชีพยิ่งขึ้นด้วยรูปแบบ Soft UI โดยนำชุดเงาละมุน (Soft Shadow และ Recessed Inset Shadow) มาประยุกต์ใช้ เพื่อสร้างมิติและความลึกให้แก่ส่วนติดต่อผู้ใช้อย่างเป็นธรรมชาติ
*   **เพิ่มระบบ 4 ธีมพาสเทลใหม่ (New Pastel Themes):** เพิ่มความหลากหลายในการแสดงผลด้วยชุดสีพาสเทลระดับพรีเมียม ซึ่งผู้ใช้สามารถเลือกใช้งานผ่านหน้าต่างการตั้งค่า (Settings) ประกอบด้วย:
    *   **Pastel Soft (ครีมพาสเทล):** การแสดงผลโทนสีครีมอบอุ่น สบายตา และเรียบง่าย
    *   **Pastel Lavender (ลาเวนเดอร์พาสเทล):** การแสดงผลโทนสีม่วงอ่อน อ่อนโยนและเป็นระบบ
    *   **Pastel Mint/Ocean (เขียวมินต์และฟ้าน้ำทะเล):** การแสดงผลโทนสีเย็น ให้ความรู้สึกสดชื่นและมีพลังสร้างสรรค์
    *   **Pastel Rose/Peach (โรสและชมพูพีช):** การแสดงผลโทนสีอบอุ่นร่วมสมัยและมีเอกลักษณ์เฉพาะตัว
*   **ปรับปรุงหน้าเข้าสู่ระบบ (Authentication UI):** ออกแบบหน้าต่างเข้าสู่ระบบใหม่ทั้งหมดภายใต้แนวคิด Soft UI พร้อมพัฒนาปุ่มสั่งการและแถบประเมินความปลอดภัยของรหัสผ่าน (Password Strength Bar) ให้แสดงผลแบบไดนามิกและสวยงามยิ่งขึ้น

---

### ความปลอดภัยและการเข้าสู่ระบบ (Security & Auth)

*   **Google Authentication:** พัฒนาระบบยืนยันตัวตนและการเข้าสู่ระบบผ่านบัญชี Google (Google Sign-In) เพื่อเพิ่มความสะดวกรวดเร็วและยกระดับความปลอดภัยด้วยมาตรฐานสากล
*   **ปรับปรุงความปลอดภัยของโครงสร้างรหัส (Core Security & Configuration):** ดำเนินการแยกไฟล์กำหนดค่าสำคัญ ได้แก่ `config.php` และคีย์ความปลอดภัยระบบ `.app_key` ออกจาก Git Repository โดยจัดทำระบบคัดกรองผ่าน `.gitignore` เพื่อป้องกันข้อมูลที่มีความอ่อนไหวรั่วไหลสู่สาธารณะ

---

### ประสิทธิภาพและการใช้งานบนมือถือ (Mobile UX & Responsiveness)

*   **Mobile-First Responsive Adjustments:** ปรับปรุงและแก้ไขการแสดงผลบนอุปกรณ์เคลื่อนที่และแท็บเล็ตให้มีความสมบูรณ์แบบยิ่งขึ้น ครอบคลุมแถบเมนูหลัก แดชบอร์ด และหน้ารายละเอียดโน้ต
*   **Notes Block Menu & Interactions:**
    *   ปรับปรุงปุ่มเพิ่มบล็อกบนอุปกรณ์เคลื่อนที่ให้แสดงผลเป็นเมนูป๊อปอัป (**Block Type Menu**) พร้อมสัญลักษณ์ไอคอน SVG แยกตามประเภทบล็อก (Text, Link, Checklist) อย่างชัดเจน
    *   ปรับปรุงค่ามิติ ความกว้าง ความสูง และคุณสมบัติ CSS Touch-friendly เพื่อรองรับการควบคุมผ่านระบบสัมผัสและการเลื่อนหน้าจอที่เสถียรบนอุปกรณ์พกพา
    *   ปรับแต่งการแสดงผลของบล็อกลิงก์ (Link Block) และบล็อกรายการตรวจสอบ (Checklist Block) ให้มีความเป็นระเบียบและใช้งานง่ายบนหน้าจอขนาดเล็ก
*   **ยกระดับความเร็วในการประมวลผล:** ปรับปรุงประสิทธิภาพของไฟล์ CSS และ JavaScript ส่วนกลางเพื่อให้ระบบโหลดข้อมูลได้รวดเร็วขึ้น พร้อมทั้งพัฒนาการตอบสนองเชิงฟิสิกส์ของการกดปุ่มและไอคอน (Feedback Transitions) ให้มีความลื่นไหลเป็นธรรมชาติ

---

### การแก้ไขข้อผิดพลาดและการปรับปรุงเสถียรภาพ (Bug Fixes & Refactoring)

*   **Response Core:** ปรับแต่งโครงสร้างระบบภายในของ `Response.php` เพื่อรับรองการส่งผ่านข้อมูล Header และการกำหนดโครงสร้าง Content-Type ให้ถูกต้องตามมาตรฐานเว็บ
*   **Profile Settings Saving:** แก้ไขปัญหาทางเทคนิคเกี่ยวกับการบันทึกตัวเลือกธีมและข้อมูลประวัติส่วนบุคคลในระบบ `SettingsController.php`
*   **Share Page View:** แก้ไขการแสดงผลหน้ารายงานลิงก์หมดอายุ (`views/share/expired.php`) และหน้าตรวจสอบการเข้าถึงข้อมูลร่วมกัน (`views/share/index.php`) ให้มีโครงสร้างการแสดงผลแบบ Responsive ที่เป็นระเบียบ สวยงาม และสอดคล้องกับธีมหลักของระบบ
